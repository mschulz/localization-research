# Raw source PDFs (198M of large binaries, 85 files). Deliberately untracked: they are
# published papers, re-retrievable, and they never delta-compress. NOTE: this means git
# holds no copy of them at all -- not here, not on origin -- so their only backup is
# whatever covers the working directory.
raw/

# macOS metadata
.DS_Store
**/.DS_Store
